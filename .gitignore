node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.env.*
!.env.example

# local npm registry config (Wix internal) — must not reach CI
.npmrc

# TypeScript incremental build artifacts
*.tsbuildinfo

# editor
.vscode/*
!.vscode/extensions.json
.idea
